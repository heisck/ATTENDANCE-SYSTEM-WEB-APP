name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

env:
  NODE_VERSION: "20"

jobs:
  lint-and-type-check:
    name: Lint & Type Check
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run TypeScript type check
        run: npx tsc --noEmit

      - name: Run ESLint
        run: npm run lint

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: lint-and-type-check
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma client
        run: npx prisma generate

      - name: Build application
        run: npm run build

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build
          path: .next
          retention-days: 1

  security:
    name: Security Checks
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run npm audit
        run: npm audit --audit-level=moderate || true
        # Continue even if vulnerabilities found, just report them

  # Placeholder for test job - uncomment when tests are added
  # test:
  #   name: Run Tests
  #   runs-on: ubuntu-latest
  #   needs: lint-and-type-check
  #   
  #   services:
  #     postgres:
  #       image: postgres:16-alpine
  #       env:
  #         POSTGRES_PASSWORD: postgres
  #         POSTGRES_DB: attendance_test
  #       options: >-
  #         --health-cmd pg_isready
  #         --health-interval 10s
  #         --health-timeout 5s
  #         --health-retries 5
  #       ports:
  #         - 5432:5432
  #   
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #   
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: ${{ env.NODE_VERSION }}
  #         cache: 'npm'
  #   
  #     - name: Install dependencies
  #       run: npm ci
  #   
  #     - name: Setup test database
  #       env:
  #         DATABASE_URL: postgresql://postgres:postgres@localhost:5432/attendance_test
  #       run: npx prisma db push --skip-generate --skip-validate
  #   
  #     - name: Run tests
  #       env:
  #         DATABASE_URL: postgresql://postgres:postgres@localhost:5432/attendance_test
  #       run: npm test -- --coverage

  success:
    name: CI Success
    runs-on: ubuntu-latest
    needs: [ lint-and-type-check, build, security ]
    if: success()
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set success status
        run: echo "✅ All CI checks passed!"
